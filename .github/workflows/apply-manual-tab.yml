name: Apply Kia Venga history import
on:
  push:
    paths:
      - 'tools/add_kia_history_import.py'
      - '.github/workflows/apply-manual-tab.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python tools/add_kia_history_import.py
      - name: Commit integration
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add app/src/main/java/com/automicosta/app/ui/AutoMiCostaApp.kt app/src/main/java/com/automicosta/app/ui/AutoMiCostaViewModel.kt
          git diff --cached --quiet || git commit -m "Integrate Kia Venga EcoGPL history import"
          git push
