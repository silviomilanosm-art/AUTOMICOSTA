name: Apply editable maintenance history
on:
  push:
    paths:
      - 'tools/add_maintenance_edit.py'
      - '.github/workflows/apply-manual-tab.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python tools/add_maintenance_edit.py
      - name: Commit integration
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add app/src/main/java/com/automicosta/app/ui/AutoMiCostaApp.kt
          git diff --cached --quiet || git commit -m "Make maintenance history editable"
          git push
